package com.hanna2100.cleannote.framework.datasource.preferences

class PreferenceKeys {
    companion object{
        // shared preference file
        const val NOTE_PREFERENCES = "com.hanna2100.cleannote.notes"
        // shared preference keys
        val NOTE_FILTER = "${NOTE_PREFERENCES}.NOTE_FILTER"
        val NOTE_ORDER = "${NOTE_PREFERENCES}.NOTE_ORDER"

    }
}